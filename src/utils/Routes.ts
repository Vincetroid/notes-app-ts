interface RoutesInterface {
  Initial: string;
  SignUp: string;
  SignIn: string;
}

const Routes: RoutesInterface = Object.freeze({
  Initial: 'Initial',
  SignUp: 'SignUp',
  SignIn: 'SignIn',
});

export default Routes;
