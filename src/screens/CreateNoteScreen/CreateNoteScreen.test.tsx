describe('Create Note tests', () => {
  test('Title text input should be empty when load CreateNoteScreen', () => {
    expect(1).toEqual(1);
  });
  test('Title text input should contain characters, not numbers or other types', () => {});
  test('Description text input should contain characters, not numbers or other types', () => {});
  test('Description text input should be empty when load CreateNoteScreen', () => {});
  test('Should loader be true when creating note', () => {});
  test('Should request create a note when tap on create note', () => {});
});
